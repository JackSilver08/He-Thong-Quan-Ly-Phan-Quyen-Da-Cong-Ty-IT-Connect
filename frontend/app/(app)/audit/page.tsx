'use client';
import {useEffect,useState} from 'react';import Layout from '@/components/Layout';import AuthGuard from '@/components/AuthGuard';import {api} from '@/lib/api';
type A={id:string;action:string;entity_type:string;entity_id?:string;details:Record<string,unknown>;created_at:string};export default function Audit(){const[items,setItems]=useState<A[]>([]);useEffect(()=>{api<{data:A[]}>('/audit-logs').then(x=>setItems(x.data)).catch(()=>{})},[]);return <AuthGuard><Layout><div className="title">Audit Logs</div><div className="subtitle">Theo dõi thay đổi và hành động nhạy cảm</div><div className="table-wrap" style={{marginTop:18}}><table className="table"><thead><tr><th>Time</th><th>Action</th><th>Entity</th><th>Details</th></tr></thead><tbody>{items.map(a=><tr key={a.id}><td>{new Date(a.created_at).toLocaleString('vi-VN')}</td><td>{a.action}</td><td>{a.entity_type}</td><td><code>{JSON.stringify(a.details)}</code></td></tr>)}</tbody></table></div></Layout></AuthGuard>}
