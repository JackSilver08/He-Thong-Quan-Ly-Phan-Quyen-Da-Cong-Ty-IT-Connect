'use client';
import {useEffect,useState} from 'react';import Layout from '@/components/Layout';import AuthGuard from '@/components/AuthGuard';import {api} from '@/lib/api';
type U={id:string;employee_code:string;full_name:string;company_name:string;resigned_at?:string;replacement_user_id?:string};export default function Resigned(){const[items,setItems]=useState<U[]>([]);useEffect(()=>{api<{data:U[]}>('/users?status=RESIGNED').then(x=>setItems(x.data)).catch(()=>{})},[]);return <AuthGuard><Layout><div className="title">Nhân viên nghỉ việc</div><div className="subtitle">Lưu lịch sử nghỉ việc và người thay thế</div><div className="table-wrap" style={{marginTop:18}}><table className="table"><thead><tr><th>Mã NV</th><th>Họ tên</th><th>Công ty</th><th>Ngày nghỉ</th><th>Người thay thế</th></tr></thead><tbody>{items.map(u=><tr key={u.id}><td>{u.employee_code}</td><td>{u.full_name}</td><td>{u.company_name}</td><td>{u.resigned_at||'—'}</td><td>{u.replacement_user_id||'—'}</td></tr>)}</tbody></table>{!items.length&&<div className="empty">Chưa có nhân viên nghỉ việc.</div>}</div></Layout></AuthGuard>}
