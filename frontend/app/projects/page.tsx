'use client';
import {useEffect,useState} from 'react';import Layout from '@/components/Layout';import AuthGuard from '@/components/AuthGuard';import {api} from '@/lib/api';
type P={id:string;code:string;name:string;company_name:string;status:string;folder_path:string};type C={id:string;name:string};
export default function Projects(){const [items,setItems]=useState<P[]>([]);const [companies,setC]=useState<C[]>([]);const [form,setForm]=useState({company_id:'',code:'',name:'',folder_path:''});const load=()=>api<{data:P[]}>('/projects').then(x=>setItems(x.data));useEffect(()=>{load();api<{data:C[]}>('/companies').then(x=>setC(x.data))},[]);const add=async()=>{await api('/projects',{method:'POST',body:JSON.stringify(form)});setForm({company_id:'',code:'',name:'',folder_path:''});load()};return <AuthGuard><Layout><div className="topbar"><div><div className="title">Dự án</div><div className="subtitle">Dự án gắn với công ty và folder File Server</div></div></div><div className="card"><div className="toolbar"><select className="select" value={form.company_id} onChange={e=>setForm({...form,company_id:e.target.value})}><option value="">Chọn công ty</option>{companies.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input className="input" placeholder="Project code" value={form.code} onChange={e=>setForm({...form,code:e.target.value})}/><input className="input" placeholder="Tên dự án" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input className="input" placeholder="/server/projects/..." value={form.folder_path} onChange={e=>setForm({...form,folder_path:e.target.value})}/><button className="button primary" onClick={add}>Thêm dự án</button></div></div><div className="table-wrap" style={{marginTop:16}}><table className="table"><thead><tr><th>Code</th><th>Dự án</th><th>Công ty</th><th>Folder</th><th>Status</th></tr></thead><tbody>{items.map(p=><tr key={p.id}><td>{p.code}</td><td>{p.name}</td><td>{p.company_name}</td><td>{p.folder_path||'—'}</td><td><span className="badge blue">{p.status}</span></td></tr>)}</tbody></table></div></Layout></AuthGuard>}
