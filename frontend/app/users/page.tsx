'use client';
import {useEffect,useState} from 'react';import Layout from '@/components/Layout';import AuthGuard from '@/components/AuthGuard';import {api} from '@/lib/api';
type U={id:string;employee_code:string;full_name:string;username:string;email:string;company_name:string;department_name?:string;role:string;status:string};
export default function Users(){const [items,setItems]=useState<U[]>([]);const [q,setQ]=useState('');const load=()=>api<{data:U[]}>(`/users?q=${encodeURIComponent(q)}`).then(x=>setItems(x.data)).catch(()=>{});useEffect(()=>{load()},[]);return <AuthGuard><Layout><div className="topbar"><div><div className="title">Nhân viên</div><div className="subtitle">Quản lý user, trạng thái và vòng đời nhân sự</div></div></div><div className="toolbar"><input className="input" placeholder="Tìm tên, mã NV, username, email..." value={q} onChange={e=>setQ(e.target.value)}/><button className="button primary" onClick={load}>Tìm kiếm</button></div><div className="table-wrap"><table className="table"><thead><tr><th>Mã NV</th><th>Họ tên</th><th>Username</th><th>Công ty</th><th>Phòng ban</th><th>Role</th><th>Status</th></tr></thead><tbody>{items.map(u=><tr key={u.id}><td>{u.employee_code}</td><td>{u.full_name}</td><td>{u.username}</td><td>{u.company_name}</td><td>{u.department_name||'—'}</td><td>{u.role}</td><td><span className={`badge ${u.status==='ACTIVE'?'green':'red'}`}>{u.status}</span></td></tr>)}</tbody></table>{!items.length&&<div className="empty">Chưa có dữ liệu.</div>}</div></Layout></AuthGuard>}
