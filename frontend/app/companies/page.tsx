'use client';
import {useEffect,useState} from 'react';import Layout from '@/components/Layout';import AuthGuard from '@/components/AuthGuard';import {api} from '@/lib/api';
type C={id:string;code:string;name:string;description:string;status:string};
export default function Companies(){const [items,setItems]=useState<C[]>([]);const [form,setForm]=useState({code:'',name:'',description:''});const load=()=>api<{data:C[]}>('/companies').then(x=>setItems(x.data)).catch(()=>{});useEffect(()=>{load()},[]);const add=async()=>{await api('/companies',{method:'POST',body:JSON.stringify(form)});setForm({code:'',name:'',description:''});load()};return <AuthGuard><Layout><div className="topbar"><div><div className="title">Công ty</div><div className="subtitle">Quản lý tenant/công ty trong hệ thống</div></div></div><div className="card"><div className="toolbar"><input className="input" placeholder="Mã công ty" value={form.code} onChange={e=>setForm({...form,code:e.target.value})}/><input className="input" placeholder="Tên công ty" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input className="input" placeholder="Mô tả" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><button className="button primary" onClick={add}>Thêm công ty</button></div></div><div className="table-wrap" style={{marginTop:16}}><table className="table"><thead><tr><th>Code</th><th>Tên</th><th>Mô tả</th><th>Status</th></tr></thead><tbody>{items.map(c=><tr key={c.id}><td>{c.code}</td><td>{c.name}</td><td>{c.description}</td><td><span className="badge green">{c.status}</span></td></tr>)}</tbody></table></div></Layout></AuthGuard>}
